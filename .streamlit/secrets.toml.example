# On Streamlit Community Cloud: App settings -> Secrets -> paste this in,
# with your real key. Locally, you can also copy this to
# .streamlit/secrets.toml instead of using a .env file if you prefer.
GEMINI_API_KEY = "your-gemini-key-here"
